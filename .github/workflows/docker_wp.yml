name: aws_deploy

on:
  push:
    branches: [ master ]
jobs:
  Build: 
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@master
      - name: Create .env file
        run: echo 'PORT=${{ secret.PORT }}' > .env

      - name: Run Build
        run:  docker-compose -d
          
